.span12
  %h1 Meetup Locations
  
- data.events.each do |id, meetup|
  - if meetup.location.present?
    - unless environment == :build && !meetup.published
      .span4
        = link_to "/#{id.dasherize}" do
          %img(src="http://placehold.it/400x260")

        %h2
          = link_to "/#{id.dasherize}" do
            = meetup.location

        %div= meetup.date.strftime("#{meetup.date.day.ordinalize} %B %Y")

        = link_to "/#{id.dasherize}", class: 'btn' do 
          View details &amp; speakers